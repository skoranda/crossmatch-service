name: crossmatch

networks:
  internal:
    external: false

volumes:
  django-db: {}
  django-static: {}
  valkey-data: {}

services:
  antares-consumer:
    image: 585193511743.dkr.ecr.us-west-2.amazonaws.com/scimma/crossmatch-service:dev
    platform: linux/x86_64
    networks:
      - internal
    build:
      context: ../crossmatch
      dockerfile: ../docker/Dockerfile
      args:
        UID: "${USERID:-1000}"
    command: bash entrypoints/run_antares_ingest.sh
    environment:
      # Dev environment settings
      MAKE_MIGRATIONS: "${MAKE_MIGRATIONS:-false}"
      DJANGO_DEBUG: "${DJANGO_DEBUG:-true}"
      LOG_LEVEL: ${LOG_LEVEL:-DEBUG}
      DEV_MODE: "${DEV_MODE:-true}" # Enable Django dev server instead of uvicorn
      # Django base configuration
      DJANGO_SUPERUSER_PASSWORD: "${DJANGO_SUPERUSER_PASSWORD:-password}"
      DJANGO_SUPERUSER_USERNAME: "${DJANGO_SUPERUSER_USERNAME:-admin}"
      DJANGO_SUPERUSER_EMAIL: "${DJANGO_SUPERUSER_EMAIL:-test@example.com}"
      # Django database configuration
      DATABASE_HOST: "${DATABASE_HOST:-django-db}"
      DATABASE_PORT: "${DATABASE_PORT:-5432}"
      DATABASE_DB: "${DATABASE_DB:-scimma_crossmatch_service}"
      DATABASE_USER: "${DATABASE_USER:-crossmatch_service_admin}"
      DATABASE_PASSWORD: "${DATABASE_PASSWORD:-password}"
      # Message broker configuration
      VALKEY_SERVICE: "${VALKEY_SERVICE:-redis}"
      VALKEY_PORT: "${VALKEY_PORT:-6379}"
      # Application configuration
      APP_ROOT_DIR: "${APP_ROOT_DIR:-/opt}"
      # ANTARES broker configuration
      ANTARES_API_KEY: "${ANTARES_API_KEY:-}"
      ANTARES_API_SECRET: "${ANTARES_API_SECRET:-}"
      ANTARES_TOPIC: "${ANTARES_TOPIC:-lsst_scimma_quality_transient}"
      ANTARES_GROUP_ID: "${ANTARES_GROUP_ID:-}"   # empty → settings.py generates timestamp-suffix
    volumes:
      - "django-static:${APP_ROOT_DIR:-/opt}/static"
      - ../crossmatch:/opt/crossmatch

  lasair-consumer:
    image: 585193511743.dkr.ecr.us-west-2.amazonaws.com/scimma/crossmatch-service:dev
    platform: linux/x86_64
    networks:
      - internal
    build:
      context: ../crossmatch
      dockerfile: ../docker/Dockerfile
      args:
        UID: "${USERID:-1000}"
    command: bash entrypoints/run_lasair_ingest.sh
    environment:
      DJANGO_DEBUG: "${DJANGO_DEBUG:-true}"
      LOG_LEVEL: ${LOG_LEVEL:-DEBUG}
      DEV_MODE: "${DEV_MODE:-true}"
      # Django base configuration
      DJANGO_SUPERUSER_PASSWORD: "${DJANGO_SUPERUSER_PASSWORD:-password}"
      DJANGO_SUPERUSER_USERNAME: "${DJANGO_SUPERUSER_USERNAME:-admin}"
      DJANGO_SUPERUSER_EMAIL: "${DJANGO_SUPERUSER_EMAIL:-test@example.com}"
      # Django database configuration
      DATABASE_HOST: "${DATABASE_HOST:-django-db}"
      DATABASE_PORT: "${DATABASE_PORT:-5432}"
      DATABASE_DB: "${DATABASE_DB:-scimma_crossmatch_service}"
      DATABASE_USER: "${DATABASE_USER:-crossmatch_service_admin}"
      DATABASE_PASSWORD: "${DATABASE_PASSWORD:-password}"
      VALKEY_SERVICE: "${VALKEY_SERVICE:-redis}"
      VALKEY_PORT: "${VALKEY_PORT:-6379}"
      APP_ROOT_DIR: "${APP_ROOT_DIR:-/opt}"
      # Lasair broker configuration
      LASAIR_KAFKA_SERVER: "${LASAIR_KAFKA_SERVER:-lasair-lsst-kafka.lsst.ac.uk:9092}"
      LASAIR_TOPIC: "${LASAIR_TOPIC:-lasair_366SCiMMA_reliability_moderate}"
      LASAIR_GROUP_ID: "${LASAIR_GROUP_ID:-}"   # empty → settings.py generates timestamp-suffix
    volumes:
      - ../crossmatch:/opt/crossmatch

  pittgoogle-consumer:
    image: 585193511743.dkr.ecr.us-west-2.amazonaws.com/scimma/crossmatch-service:dev
    platform: linux/x86_64
    networks:
      - internal
    build:
      context: ../crossmatch
      dockerfile: ../docker/Dockerfile
      args:
        UID: "${USERID:-1000}"
    command: bash entrypoints/run_pittgoogle_ingest.sh
    environment:
      DJANGO_DEBUG: "${DJANGO_DEBUG:-true}"
      LOG_LEVEL: ${LOG_LEVEL:-DEBUG}
      DEV_MODE: "${DEV_MODE:-true}"
      # Django base configuration
      DJANGO_SUPERUSER_PASSWORD: "${DJANGO_SUPERUSER_PASSWORD:-password}"
      DJANGO_SUPERUSER_USERNAME: "${DJANGO_SUPERUSER_USERNAME:-admin}"
      DJANGO_SUPERUSER_EMAIL: "${DJANGO_SUPERUSER_EMAIL:-test@example.com}"
      # Django database configuration
      DATABASE_HOST: "${DATABASE_HOST:-django-db}"
      DATABASE_PORT: "${DATABASE_PORT:-5432}"
      DATABASE_DB: "${DATABASE_DB:-scimma_crossmatch_service}"
      DATABASE_USER: "${DATABASE_USER:-crossmatch_service_admin}"
      DATABASE_PASSWORD: "${DATABASE_PASSWORD:-password}"
      VALKEY_SERVICE: "${VALKEY_SERVICE:-redis}"
      VALKEY_PORT: "${VALKEY_PORT:-6379}"
      APP_ROOT_DIR: "${APP_ROOT_DIR:-/opt}"
      # Pitt-Google broker configuration
      PITTGOOGLE_TOPIC: "${PITTGOOGLE_TOPIC:-lsst-alerts-json}"
      PITTGOOGLE_SUBSCRIPTION: "${PITTGOOGLE_SUBSCRIPTION:-scimma-crossmatch-lsst-alerts-json}"
      PITTGOOGLE_PUBLISHER_PROJECT: "${PITTGOOGLE_PUBLISHER_PROJECT:-pitt-alert-broker}"
      # Broker filter standard (see scimma_crossmatch_service_design.md §2.2)
      MIN_DIASOURCE_RELIABILITY: "${MIN_DIASOURCE_RELIABILITY:-0.6}"
      # GCP auth
      GOOGLE_CLOUD_PROJECT: "${GOOGLE_CLOUD_PROJECT:-}"
      GOOGLE_APPLICATION_CREDENTIALS: /var/run/secrets/gcp/key.json
    volumes:
      - ../crossmatch:/opt/crossmatch
      - ${GCP_KEY_FILE:-./secrets/gcp-pittgoogle-key.json}:/var/run/secrets/gcp/key.json:ro

  celery-worker:
    image: 585193511743.dkr.ecr.us-west-2.amazonaws.com/scimma/crossmatch-service:dev
    platform: linux/x86_64
    build:
      context: ../crossmatch
      dockerfile: ../docker/Dockerfile
      args:
        UID: "${USERID:-1000}"
    command: bash entrypoints/run_celery_worker.sh alerts
    # Cap retries so a startup-time failure (e.g., Dask version drift caught
    # by the worker_init check) stops looping after a handful of attempts
    # instead of restarting forever and flooding the logs.
    restart: "on-failure:5"
    depends_on:
      valkey:
        condition: service_healthy
    networks:
      - internal
    deploy:
      mode: replicated
      replicas: 2
      resources:
        reservations:
          cpus: '0.25'
          memory: 1G
        limits:
          cpus: "${CELERY_WORKER_LIMIT_CPUS:-4.0}"
          memory: ${CELERY_WORKER_LIMIT_MEMORY:-4G}
    environment:
      # Dev environment settings
      DJANGO_DEBUG: "${DJANGO_DEBUG:-true}"
      DEV_MODE: "${DEV_MODE:-true}" # Enable Django dev server instead of uvicorn
      CELERY_LOG_LEVEL: ${CELERY_LOG_LEVEL:-INFO}
      CELERY_CONCURRENCY: ${CELERY_CONCURRENCY:-4}
      # Django database configuration
      DATABASE_HOST: "${DATABASE_HOST:-django-db}"
      DATABASE_PORT: "${DATABASE_PORT:-5432}"
      DATABASE_DB: "${DATABASE_DB:-scimma_crossmatch_service}"
      DATABASE_USER: "${DATABASE_USER:-crossmatch_service_admin}"
      DATABASE_PASSWORD: "${DATABASE_PASSWORD:-password}"
      # Message broker configuration
      VALKEY_SERVICE: "${VALKEY_SERVICE:-redis}"
      VALKEY_PORT: "${VALKEY_PORT:-6379}"
      # Application configuration
      APP_ROOT_DIR: "${APP_ROOT_DIR:-/opt}"
      # Batch crossmatch thresholds
      #CROSSMATCH_BATCH_MAX_WAIT_SECONDS: "${CROSSMATCH_BATCH_MAX_WAIT_SECONDS:-900}"
      CROSSMATCH_BATCH_MAX_WAIT_SECONDS: "${CROSSMATCH_BATCH_MAX_WAIT_SECONDS:-60}"
      CROSSMATCH_BATCH_MAX_SIZE: "${CROSSMATCH_BATCH_MAX_SIZE:-100000}"
      # LSDB crossmatch settings
      GAIA_HATS_URL: "${GAIA_HATS_URL:-s3://stpubdata/gaia/gaia_dr3/public/hats}"
      DES_HATS_URL: "${DES_HATS_URL:-https://data.lsdb.io/hats/des/des_y6_gold}"
      DELVE_HATS_URL: "${DELVE_HATS_URL:-https://data.lsdb.io/hats/delve/delve_dr3_gold}"
      SKYMAPPER_HATS_URL: "${SKYMAPPER_HATS_URL:-https://data.lsdb.io/hats/skymapper_dr4/catalog}"
      CROSSMATCH_RADIUS_ARCSEC: "${CROSSMATCH_RADIUS_ARCSEC:-1.0}"
      # Remote Dask scheduler (empty = local Dask scheduler)
      DASK_SCHEDULER_ADDRESS: "${DASK_SCHEDULER_ADDRESS:-}"
      # Higher default than settings.py (300s) — cold EXTRA_PIP_PACKAGES installs
      # on the dask-scheduler/worker containers regularly take several minutes.
      DASK_VERSION_CHECK_TIMEOUT_SECONDS: "${DASK_VERSION_CHECK_TIMEOUT_SECONDS:-600}"
      # Hopskotch publisher configuration
      HOPSKOTCH_BROKER_URL: "${HOPSKOTCH_BROKER_URL:-kafka://kafka.scimma.org}"
      HOPSKOTCH_TOPIC: "${HOPSKOTCH_TOPIC:-}"
      HOPSKOTCH_USERNAME: "${HOPSKOTCH_USERNAME:-}"
      HOPSKOTCH_PASSWORD: "${HOPSKOTCH_PASSWORD:-}"
    volumes:
      - ../crossmatch:/opt/crossmatch

  celery-beat:
    restart: on-failure
    depends_on:
      valkey:
        condition: service_healthy
    image: 585193511743.dkr.ecr.us-west-2.amazonaws.com/scimma/crossmatch-service:dev
    build:
      context: ../crossmatch
      dockerfile: ../docker/Dockerfile
      args:
        UID: "${USERID:-1000}"
    command: bash entrypoints/run_celery_beat.sh
    environment:
      DJANGO_DEBUG: "${DJANGO_DEBUG:-true}"
      DEV_MODE: "${DEV_MODE:-true}" # Enable Django dev server instead of uvicorn
      CELERY_LOG_LEVEL: ${CELERY_LOG_LEVEL:-INFO}
      # Django database configuration
      DATABASE_HOST: "${DATABASE_HOST:-django-db}"
      DATABASE_PORT: "${DATABASE_PORT:-5432}"
      DATABASE_DB: "${DATABASE_DB:-scimma_crossmatch_service}"
      DATABASE_USER: "${DATABASE_USER:-crossmatch_service_admin}"
      DATABASE_PASSWORD: "${DATABASE_PASSWORD:-password}"
      # Message broker configuration
      VALKEY_SERVICE: "${VALKEY_SERVICE:-redis}"
      VALKEY_PORT: "${VALKEY_PORT:-6379}"
      # Batch crossmatch thresholds
      CROSSMATCH_BATCH_MAX_WAIT_SECONDS: "${CROSSMATCH_BATCH_MAX_WAIT_SECONDS:-900}"
      CROSSMATCH_BATCH_MAX_SIZE: "${CROSSMATCH_BATCH_MAX_SIZE:-100000}"
    networks:
      - internal
    volumes:
      - ../crossmatch:/opt/crossmatch

  flower:
    image: 585193511743.dkr.ecr.us-west-2.amazonaws.com/scimma/crossmatch-service:dev
    platform: linux/x86_64
    build:
      context: ../crossmatch
      dockerfile: ../docker/Dockerfile
      args:
        UID: "${USERID:-1000}"
    command: bash entrypoints/run_flower.sh
    networks:
      internal:
        aliases:
          - ${FLOWER_HOST:-flower}
    ports:
      - 127.0.0.1:${FLOWER_PORT:-8888}:${FLOWER_PORT:-8888}
    environment:
      # Message broker configuration
      VALKEY_SERVICE: "${VALKEY_SERVICE:-redis}"
      VALKEY_PORT: "${VALKEY_PORT:-6379}"
      # Flower configuration
      FLOWER_LOG_LEVEL: "${FLOWER_LOG_LEVEL:-INFO}"
      FLOWER_URL_PREFIX: "${FLOWER_URL_PREFIX:-}"
      FLOWER_HOST: "${FLOWER_HOST:-flower}"
      FLOWER_PORT: "${FLOWER_PORT:-8888}"
      # Django database configuration
      DATABASE_HOST: "${DATABASE_HOST:-django-db}"
      DATABASE_PORT: "${DATABASE_PORT:-5432}"
      DATABASE_DB: "${DATABASE_DB:-scimma_crossmatch_service}"
      DATABASE_USER: "${DATABASE_USER:-crossmatch_service_admin}"
      DATABASE_PASSWORD: "${DATABASE_PASSWORD:-password}"

  django-db:
    image: postgres:18.3
    platform: linux/x86_64
    restart: always
    networks:
      internal:
        aliases:
          - ${DATABASE_HOST:-django-db}
    environment:
      POSTGRES_USER: "${DATABASE_USER:-crossmatch_service_admin}"
      POSTGRES_PASSWORD: "${DATABASE_PASSWORD:-password}"
      POSTGRES_DB: "${DATABASE_DB:-scimma_crossmatch_service}"
    volumes:
      - django-db:/var/lib/postgresql

  valkey:
    image: valkey/valkey:9.0.3
    command: [ "valkey-server", "/usr/local/etc/valkey/valkey.conf" ]
    ports:
      - 127.0.0.1:${VALKEY_PORT:-6379}:6379
    networks:
      internal:
        aliases:
          - "${VALKEY_SERVICE:-redis}"
    volumes:
      - valkey-data:/data
      - ./valkey:/usr/local/etc/valkey:ro
    healthcheck:
      test: [ "CMD-SHELL", "valkey-cli -p ${VALKEY_PORT:-6379} ping | grep PONG"]
      interval: 20s
      timeout: 10s
      retries: 3
      start_interval: 5s
      start_period: 5s

  local-kafka:
    image: scimma/server:latest
    command: ["--noSecurity"]
    hostname: local-kafka
    profiles:
      - local-kafka
    networks:
      internal:
        aliases:
          - local-kafka
    ports:
      - 127.0.0.1:9092:9092
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 9092 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 30s

  local-kafka-init:
    image: scimma/server:latest
    profiles:
      - local-kafka
    depends_on:
      local-kafka:
        condition: service_healthy
    networks:
      - internal
    entrypoint: /usr/bin/kafka-topics
    command:
      - --bootstrap-server
      - local-kafka:9092
      - --create
      - --if-not-exists
      - --topic
      - ${HOPSKOTCH_TOPIC:-crossmatch-test}
      - --partitions
      - "1"
      - --replication-factor
      - "1"
    restart: "no"

  dask-scheduler:
    image: ghcr.io/dask/dask:2026.1.2-py3.12
    command: ["dask-scheduler"]
    hostname: dask-scheduler
    profiles:
      - dask-scheduler
    networks:
      internal:
        aliases:
          - dask-scheduler
    ports:
      - 127.0.0.1:8786:8786
      - 127.0.0.1:8787:8787
    environment:
      EXTRA_PIP_PACKAGES: "lsdb==0.9.0 numpy==2.4.2 pandas==2.3.3 s3fs"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8787/health')"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 60s

  dask-worker:
    image: ghcr.io/dask/dask:2026.1.2-py3.12
    command: ["dask-worker", "dask-scheduler:8786"]
    profiles:
      - dask-scheduler
    depends_on:
      dask-scheduler:
        condition: service_healthy
    networks:
      - internal
    environment:
      EXTRA_PIP_PACKAGES: "lsdb==0.9.0 numpy==2.4.2 pandas==2.3.3 s3fs"
