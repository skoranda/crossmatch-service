antares-client[subscriptions]
astropy
django>=6.0,<6.1
django_celery_results
django-celery-beat
django-tables2
flower
hop-client
httpx
confluent_kafka
lasair
lsdb==0.9.0
# Pin hats: lsdb 0.9.0 calls HealpixDataset(..., original_schema=...), but hats
# 0.9.1 removed that constructor arg. lsdb's loose `hats<0.10,>=0.9.0` lets pip
# pull 0.9.1/0.9.2, which makes every lsdb.open_catalog() raise TypeError
# (unexpected kwarg 'original_schema') -> all crossmatches silently fail. 0.9.0
# is the only <0.10 hats compatible with lsdb 0.9.0.
hats==0.9.0
pittgoogle-client
numpy==2.4.2
pandas==2.3.3
tornado==6.5.4
dask[complete]==2026.1.2
s3fs
prometheus-client
psycopg[binary]
redis
structlog
valkey
watchdog
